public class SummationOfNumber {
    
    public static void sumOfNumber(integer a)
    {
        integer sum=0;
        for(integer i = 0;i<=a;i++)
        {
            sum = sum+i;
        }
        system.debug(sum);
    }

}