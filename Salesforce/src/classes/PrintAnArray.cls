public class PrintAnArray {
    
    public static void uptoInteger(integer a)
    {
        
        List<Integer> ints = new Integer[a];

        //Integer[] myList = new List<Integer>(a);
		for(integer i =0;i<=ints.size();i++)
        {
            system.debug('print');
        }   
    }

}